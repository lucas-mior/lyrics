## 1. Clean up the current vocals extraction API

* Create one public function for extracting vocals from an input song.
* Make input song path, output vocals path, MDX model path, and temp directory explicit parameters.
* Return structured errors from production code instead of aborting.
* Keep `ASSERT` usage inside tests only.
* Add a synthetic test that generates a tiny WAV file and confirms the extraction function creates a decodable output when model assets are available.
* Add a Maxwell fixture smoke path that can use `maxwell.flac` as the input song.

## 2. Add reusable audio fixture helpers

* Add test helpers to generate temporary WAV files.
* Support configurable duration, sample rate, channels, and sine frequency.
* Add helpers to inspect audio duration, channel count, codec, and sample rate.
* Use generated audio for ordinary tests.
* Add fixture helpers that can also inspect `maxwell.flac` and `maxwell_vocals.opus`.

## 3. Split generic ONNX Runtime helpers from vocals code

* Move ONNX env/session/tensor setup into a reusable module.
* Keep MDX/vocals-specific tensor layout in the vocals module.
* Add helpers for loading sessions, querying names/shapes, creating tensors, running inference, and freeing resources.
* Update vocals extraction to use this shared ONNX layer.
* Add tests using fake or minimal ONNX paths where possible, with real ONNX execution skipped cleanly when assets are missing.

## 4. Consolidate build dependency detection

* Ensure every module test receives ONNX Runtime `pkg-config --cflags --libs`.
* Ensure FFTW flags are added whenever FFTW-dependent code is compiled.
* Add FFmpeg library detection for future direct decode/resample code.
* Fix transitive dependency detection for included `.c` modules.
* Add build-script tests that inspect generated compiler commands for the required flags.

## 5. Add a top-level pipeline state object

* Add a pipeline struct that owns config, paths, temp files, model paths, stage outputs, and cleanup state.
* Centralize temporary file creation.
* Centralize temporary file cleanup.
* Route current vocals extraction through this object.
* Add synthetic tests for temp path creation and cleanup.
* Add a Maxwell fixture test that configures the pipeline with `maxwell.flac`, `maxwell_vocals.opus`, `maxwell.txt`, and expected `maxwell.lrc`.

## 6. Add UTF-8 lyrics loading

* Add a lyrics module.
* Load `.txt` lyrics from disk.
* Strip UTF-8 BOM.
* Normalize CRLF and CR to LF.
* Preserve original line boundaries.
* Reject empty lyrics.
* Add synthetic tests that create temporary lyric files with BOM, CRLF, Unicode, blank lines, and trailing newline cases.
* Add a Maxwell test that loads `maxwell.txt` and verifies line preservation.

## 7. Implement basic lyrics normalization

* Port the Python prototype’s lowercasing behavior.
* Normalize whitespace.
* Remove or transform punctuation the same way as the prototype.
* Preserve a mapping from normalized characters to original source lines.
* Add synthetic tests with punctuation-heavy strings.
* Add a Maxwell test that normalizes `maxwell.txt` and checks that blank-line separation does not break mapping.

## 8. Implement normalized-offset to lyric-line mapping

* Store normalized start/end offsets for every original lyric line.
* Map normalized character offsets back to original line indexes.
* Handle blank lines explicitly.
* Handle punctuation-only lines explicitly.
* Add synthetic tests for repeated lines, blank lines, and punctuation-only lines.
* Add Maxwell mapping tests using each line from `maxwell.txt`.

## 9. Add CTC model asset lookup

* Add config fields for CTC model path and tokenizer path.
* Validate required files before inference.
* Keep model validation separate from alignment logic.
* Add synthetic tests that create temporary fake model/tokenizer files.
* Add missing-file tests with clear errors.
* Do not require the real CTC model for normal tests.

## 10. Load a minimal vocabulary/tokenizer format

* Add vocabulary loading from a simple text or JSON format that the real tokenizer can be converted into.
* Build token-to-ID and ID-to-token tables.
* Store blank token ID.
* Store unknown-token behavior if needed.
* Add synthetic tests that generate a tiny vocabulary file with blank, space, and a few letters.
* Later, add an optional real-tokenizer test when the actual CTC tokenizer asset is available.

## 11. Convert normalized lyrics into CTC token IDs

* Implement text-to-token conversion using the loaded vocabulary.
* Store token-to-normalized-character offset mappings.
* Reject unsupported characters unless an unknown token exists.
* Add synthetic tests using generated vocabularies and in-code lyric strings.
* Add a Maxwell test that tokenizes normalized `maxwell.txt` with a test vocabulary or real vocabulary when available.

## 12. Add audio decoding for aligner input

* Decode an audio file into float samples.
* Convert stereo to mono.
* Resample to the CTC model sample rate.
* Normalize samples to `float`.
* Add synthetic tests that generate WAV files at different sample rates and channel counts.
* Add Maxwell tests that decode `maxwell_vocals.opus` and verify duration, channel conversion, sample rate conversion, and float range.

## 13. Prepare CTC model input tensors

* Add a CTC model module.
* Convert decoded float audio into the model’s expected input layout.
* Validate rank, dtype, and sample count.
* Add tests using generated float arrays instead of audio files.
* Add a Maxwell-shaped test using decoded `maxwell_vocals.opus` samples, but without requiring real inference.

## 14. Run CTC model inference behind an interface

* Add an inference interface that accepts prepared audio and returns emissions.
* Implement the production ONNX backend.
* Implement a fake backend for tests.
* Make ordinary tests use the fake backend by default.
* Add tests that verify frame count and vocabulary size handling.
* Add optional real-backend tests that run only when the real CTC model path is configured.

## 15. Convert emissions to log-probabilities if needed

* Determine from the Python prototype whether the model output is logits, probabilities, or log-probabilities.
* Add stable log-softmax if needed.
* Allow bypassing conversion when emissions are already in the expected domain.
* Add synthetic matrix tests with known expected ordering.
* Keep this independent from real audio and real ONNX inference.

## 16. Implement CTC trellis allocation and initialization

* Allocate `num_frames × num_tokens` trellis storage.
* Initialize start states.
* Fill impossible states with negative infinity.
* Add bounds checks.
* Add synthetic tests for tiny dimensions and invalid zero-size cases.

## 17. Implement CTC trellis forward scoring

* Fill the trellis frame by frame.
* Support blank transitions.
* Support token-advance transitions.
* Store only scores at this stage.
* Add synthetic emissions where the best path is obvious by construction.
* Do not combine this with backtracking yet.

## 18. Implement CTC backtracking

* Walk backward through the filled trellis.
* Recover the best frame/token path.
* Handle repeated tokens.
* Detect impossible alignments.
* Add synthetic tests with forced known paths.
* Keep this separate from token-span conversion.

## 19. Convert backtracked paths into token spans

* Collapse frame/token path entries into token spans.
* Store token ID, start frame, end frame, and score.
* Convert frame indexes into seconds using supplied frame duration.
* Add synthetic tests for repeated letters, blanks, and short alignments.
* Add a fake-emission Maxwell test that produces predictable token timings for `maxwell.txt`.

## 20. Add full synthetic CTC alignment tests

* Generate a tiny vocabulary in code.
* Generate normalized lyrics in code.
* Generate artificial emissions that strongly favor known timings.
* Run tokenization, trellis scoring, backtracking, and token-span conversion.
* Verify token timestamps exactly or within tiny tolerance.
* Keep this as the main non-model correctness test.

## 21. Parse expected `.lrc` fixture data

* Add an LRC parser for tests.
* Parse timestamped lines from `maxwell.lrc`.
* Preserve untimestamped blank lines.
* Convert timestamps into seconds.
* Add tests for timestamp parsing, blank lines, malformed timestamps, and duplicate timestamps.
* Use parsed `maxwell.lrc` as the expected real fixture oracle.

## 22. Convert token spans into word spans

* Group token spans into words using normalized text offsets.
* Preserve word start/end timestamps.
* Handle spaces removed or normalized during preprocessing.
* Add synthetic tests using generated lyrics and token spans.
* Add Maxwell tests that verify words map back to the right original lyric lines.

## 23. Convert word spans into lyric-line timestamps

* Map word spans back to original lyric lines.
* Assign each line a start timestamp.
* Skip lines with no alignable tokens.
* Preserve or skip blank lines according to the project’s chosen LRC behavior.
* Add synthetic tests for multi-line lyrics, repeated lines, and blank lines.
* Add a Maxwell comparison test against parsed `maxwell.lrc` timestamps, using tolerance rather than exact equality.

## 24. Implement `.lrc` timestamp formatting

* Format timestamps as `[mm:ss.xx]`.
* Define and implement rounding behavior.
* Preserve original lyric text after the timestamp.
* Add synthetic tests for zero, sub-second, minute boundary, long duration, and rounding cases.
* Add Maxwell tests that check formatting is compatible with `maxwell.lrc`.

## 25. Implement `.lrc` file writing

* Write generated LRC lines to a temporary file.
* Rename atomically to the output path.
* Preserve UTF-8.
* Add synthetic tests that compare exact generated file contents.
* Add overwrite behavior tests.
* Add a Maxwell test that writes a generated `.lrc` and compares it structurally against `maxwell.lrc`.

## 26. Wire a full synthetic end-to-end alignment pipeline

* Create lyrics in code.
* Generate temporary sine-wave audio.
* Use a fake CTC backend that returns deterministic emissions.
* Run lyrics loading, normalization, tokenization, fake emissions, CTC alignment, line timestamp generation, and LRC writing.
* Verify exact `.lrc` output.
* Keep this test independent from real songs, real vocals, and real models.

## 27. Wire a Maxwell fixture end-to-end validation path

* Use `maxwell.txt` as lyrics input.
* Use `maxwell_vocals.opus` as already-extracted vocals input.
* Decode/resample vocals.
* Use either:

  * fake emissions for deterministic pipeline validation, or
  * real CTC model inference when model assets are configured.
* Generate `.lrc`.
* Compare generated line timestamps against `maxwell.lrc` with a tolerance.
* Report per-line timestamp differences in test failures.

## 28. Wire the real production full pipeline

* Add the production function that accepts original song path, lyrics path, output LRC path, vocals model path, CTC model path, and tokenizer path.
* Run vocals extraction from the original song.
* Load and normalize lyrics.
* Tokenize lyrics.
* Decode/resample extracted vocals.
* Run real CTC inference.
* Perform CTC alignment.
* Generate lyric-line timestamps.
* Write `.lrc`.
* Add a production-path test that can use `maxwell.flac` plus `maxwell.txt`, but skips cleanly unless both required neural models are configured.
